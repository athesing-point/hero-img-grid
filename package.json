{
  "name": "hero-img-grid",
  "version": "1.0.0",
  "description": "Hero image grid with parallax effects",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
